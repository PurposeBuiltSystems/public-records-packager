{
  "name": "public-records-packager",
  "version": "1.0.0",
  "description": "Outlook add-in that searches your mail for responsive records and builds a numbered, manifest-indexed records bundle in OneDrive.",
  "private": true,
  "scripts": {
    "validate": "office-addin-manifest validate manifest.xml",
    "test": "node tools/check-undeclared.js && node test/packager.test.js",
    "check": "node tools/check-undeclared.js"
  },
  "author": "Matthew Miller",
  "license": "MIT",
  "devDependencies": {
    "office-addin-manifest": "^2.0.4",
    "acorn": "^8.16.0"
  }
}
